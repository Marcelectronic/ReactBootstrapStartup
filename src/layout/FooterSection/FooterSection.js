import React from "react";
import Footer from '../../UI/Footer/Footer'

const FooterSection = () => {
	return (
		<footer>
			<Footer></Footer>
		</footer>
	);
};

export default React.memo(FooterSection);
